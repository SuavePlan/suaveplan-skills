---
name: testing-integration
description: Use when implementing or debugging code that uses @suaveplan/testing-integration: Core integration test infrastructure: ServiceFixture<T> interface, TestEnvironment, createIntegrationSetup() factory, HealthPoller, skipIfUnavailable() helper.
---

# @suaveplan/testing-integration

Core integration test infrastructure for Vitest: fixture lifecycle management, health polling, and the global-setup bridge.

## When to use this skill

- Implementing or debugging code that imports `@suaveplan/testing-integration`.
- The task touches: see package README.
- You need API guidance for this package's exports.

## Quick reference

```ts
import { /* see API Reference below */ } from "@suaveplan/testing-integration";
```

## Dependencies

Workspace dependencies: `@suaveplan/types`

## Where to read more

This skill is intentionally short. The package's own documentation is the authoritative source — read these in order:

1. **`packages/dev/testing-integration/README.md`** — overview, install, quick-start, module index
2. **`packages/dev/testing-integration/src/**/*.md`** — co-located docs per implementation file (Purpose, Features, Basic + Advanced examples, full API reference, Implementation notes — all packages in genesis ship ≥200 words per module)
3. **`packages/dev/testing-integration/package.json`** `exports` map — every subpath you can import from

When the user asks an API question, read the relevant co-located `.md` first; do not answer from training-data memory.

## See also

- `@suaveplan/types`

---

_This SKILL.md is auto-generated by `suaveplan-skills/scripts/generate-skills.ts` from the package's README. Do not edit by hand — re-run the generator after the README changes._
