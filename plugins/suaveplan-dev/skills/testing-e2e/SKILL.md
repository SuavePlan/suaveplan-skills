---
name: testing-e2e
description: Use when implementing or debugging code that uses @suaveplan/testing-e2e: Playwright E2E utilities — config factory, page objects, fixtures, helpers, structured errors.
---

# @suaveplan/testing-e2e

Playwright E2E testing utilities for the Suaveplan monorepo. Provides a config factory with WSL2/CI-safe defaults, an abstract `PageObject` base class, extended test fixtures (`apiContext`, `freshPage`), browser-context helpers (clipboard mock, geolocation mock, network-idle wait, screenshot-on-failure), and structured error types. The package has no `@suaveplan/*` runtime dependencies — it is a Layer 0 utility that anything in the stack can depend on without introducing cycles.

## When to use this skill

- Implementing or debugging code that imports `@suaveplan/testing-e2e`.
- The task touches: see package README.
- You need API guidance for this package's exports.

## Quick reference

```ts
import { /* see API Reference below */ } from "@suaveplan/testing-e2e";
```

## Dependencies

Workspace dependencies: _none_

## Where to read more

This skill is intentionally short. The package's own documentation is the authoritative source — read these in order:

1. **`packages/dev/testing-e2e/README.md`** — overview, install, quick-start, module index
2. **`packages/dev/testing-e2e/src/**/*.md`** — co-located docs per implementation file (Purpose, Features, Basic + Advanced examples, full API reference, Implementation notes — all packages in genesis ship ≥200 words per module)
3. **`packages/dev/testing-e2e/package.json`** `exports` map — every subpath you can import from

When the user asks an API question, read the relevant co-located `.md` first; do not answer from training-data memory.

## See also

- The marketplace root: `../../../README.md`

---

_This SKILL.md is auto-generated by `suaveplan-skills/scripts/generate-skills.ts` from the package's README. Do not edit by hand — re-run the generator after the README changes._
