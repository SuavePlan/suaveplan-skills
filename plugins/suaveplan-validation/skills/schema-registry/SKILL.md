---
name: schema-registry
description: Use when implementing or debugging code that uses @suaveplan/schema-registry: Versioned schema registry with forward/backward compatibility checking for evolving data contracts.
---

# @suaveplan/schema-registry

Versioned schema registry with forward/backward/full compatibility checking for evolving data contracts. Register schemas by subject name, enforce evolution rules, and detect breaking changes before they reach production.

## When to use this skill

- Implementing or debugging code that imports `@suaveplan/schema-registry`.
- The task touches: see package README.
- You need API guidance for this package's exports.

## Quick reference

```ts
import { /* see API Reference below */ } from "@suaveplan/schema-registry";
```

## Dependencies

Workspace dependencies: `@suaveplan/error`, `@suaveplan/types`

## Where to read more

This skill is intentionally short. The package's own documentation is the authoritative source — read these in order:

1. **`packages/validation/schema-registry/README.md`** — overview, install, quick-start, module index
2. **`packages/validation/schema-registry/src/**/*.md`** — co-located docs per implementation file (Purpose, Features, Basic + Advanced examples, full API reference, Implementation notes — all packages in genesis ship ≥200 words per module)
3. **`packages/validation/schema-registry/package.json`** `exports` map — every subpath you can import from

When the user asks an API question, read the relevant co-located `.md` first; do not answer from training-data memory.

## See also

- `@suaveplan/error`
- `@suaveplan/types`

---

_This SKILL.md is auto-generated by `suaveplan-skills/scripts/generate-skills.ts` from the package's README. Do not edit by hand — re-run the generator after the README changes._
