---
name: events-transport
description: Use when implementing or debugging code that uses @suaveplan/events-transport: Cross-context event bridges for @suaveplan/events: MessageChannel (at-least-once with ack) and BroadcastChannel (at-most-once) transports with nonce dedup, W3C trace context, and AbortSignal support.
---

# @suaveplan/events-transport

Cross-context event bridges for `@suaveplan/events`: `MessageChannel` (at-least-once with ack/retry) and `BroadcastChannel` (at-most-once, fan-out) transports with nonce deduplication and AbortSignal support.

## When to use this skill

- Implementing or debugging code that imports `@suaveplan/events-transport`.
- The task touches: see package README.
- You need API guidance for this package's exports.

## Quick reference

```ts
import { /* see API Reference below */ } from "@suaveplan/events-transport";
```

## Dependencies

Workspace dependencies: `@suaveplan/error`, `@suaveplan/events`, `@suaveplan/types`

## Where to read more

This skill is intentionally short. The package's own documentation is the authoritative source — read these in order:

1. **`packages/events/events-transport/README.md`** — overview, install, quick-start, module index
2. **`packages/events/events-transport/src/**/*.md`** — co-located docs per implementation file (Purpose, Features, Basic + Advanced examples, full API reference, Implementation notes — all packages in genesis ship ≥200 words per module)
3. **`packages/events/events-transport/package.json`** `exports` map — every subpath you can import from

When the user asks an API question, read the relevant co-located `.md` first; do not answer from training-data memory.

## See also

- `@suaveplan/error`
- `@suaveplan/events`
- `@suaveplan/types`

---

_This SKILL.md is auto-generated by `suaveplan-skills/scripts/generate-skills.ts` from the package's README. Do not edit by hand — re-run the generator after the README changes._
