---
name: storage
description: Use when implementing or debugging code that uses @suaveplan/storage: Enterprise key-value storage abstractions: AbstractStorage (StorageLike + streaming + hooks + OTel), MemoryStorage (TTL, zero-copy streams), NamespacedStorage, StorageFactory with module-augmentation provider registry, structured STORAGE.
---

# @suaveplan/storage

Enterprise key-value storage abstractions for the genesis monorepo. Provides `AbstractStorage` (implementing `StorageLike`), `MemoryStorage` with TTL and zero-copy streaming, `NamespacedStorage` with scoped operations, and `StorageFactory` with a module-augmentation provider registry.

## When to use this skill

- Implementing or debugging code that imports `@suaveplan/storage`.
- The task touches: see package README.
- You need API guidance for this package's exports.

## Quick reference

```ts
import { /* see API Reference below */ } from "@suaveplan/storage";
```

## Dependencies

Workspace dependencies: `@suaveplan/error`, `@suaveplan/events`, `@suaveplan/types`

## Where to read more

This skill is intentionally short. The package's own documentation is the authoritative source — read these in order:

1. **`packages/storage/storage/README.md`** — overview, install, quick-start, module index
2. **`packages/storage/storage/src/**/*.md`** — co-located docs per implementation file (Purpose, Features, Basic + Advanced examples, full API reference, Implementation notes — all packages in genesis ship ≥200 words per module)
3. **`packages/storage/storage/package.json`** `exports` map — every subpath you can import from

When the user asks an API question, read the relevant co-located `.md` first; do not answer from training-data memory.

## See also

- `@suaveplan/error`
- `@suaveplan/events`
- `@suaveplan/types`

---

_This SKILL.md is auto-generated by `suaveplan-skills/scripts/generate-skills.ts` from the package's README. Do not edit by hand — re-run the generator after the README changes._
