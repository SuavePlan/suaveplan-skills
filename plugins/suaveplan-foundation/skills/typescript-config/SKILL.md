---
name: typescript-config
description: Use when implementing or debugging code that uses @suaveplan/typescript-config: Shared TypeScript configuration presets for the Suaveplan monorepo.
---

# @suaveplan/typescript-config

Shared TypeScript configuration presets for the Suaveplan monorepo. This package ships composable JSON presets consumed via `"extends"` across every workspace package — no runtime code, no build step, zero dependencies.

## When to use this skill

- Implementing or debugging code that imports `@suaveplan/typescript-config`.
- The task touches: see package README.
- You need API guidance for this package's exports.

## Quick reference

```ts
import { /* see API Reference below */ } from "@suaveplan/typescript-config";
```

## Dependencies

Workspace dependencies: _none_

## Where to read more

This skill is intentionally short. The package's own documentation is the authoritative source — read these in order:

1. **`packages/foundation/typescript-config/README.md`** — overview, install, quick-start, module index
2. **`packages/foundation/typescript-config/src/**/*.md`** — co-located docs per implementation file (Purpose, Features, Basic + Advanced examples, full API reference, Implementation notes — all packages in genesis ship ≥200 words per module)
3. **`packages/foundation/typescript-config/package.json`** `exports` map — every subpath you can import from

When the user asks an API question, read the relevant co-located `.md` first; do not answer from training-data memory.

## See also

- The marketplace root: `../../../README.md`

---

_This SKILL.md is auto-generated by `suaveplan-skills/scripts/generate-skills.ts` from the package's README. Do not edit by hand — re-run the generator after the README changes._
