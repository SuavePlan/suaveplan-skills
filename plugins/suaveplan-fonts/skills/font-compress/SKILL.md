---
name: font-compress
description: Use when implementing or debugging code that uses @suaveplan/font-compress: BTF (Binary TypeFace) binary packing and unpacking.
---

# @suaveplan/font-compress

BTF (Binary TypeFace) binary packing and unpacking. Converts `TypefaceFont` (typeface.js JSON format) to a compact `Uint8Array` using dictionary-based integer compression, and reverses it.

## When to use this skill

- Implementing or debugging code that imports `@suaveplan/font-compress`.
- The task touches: see package README.
- You need API guidance for this package's exports.

## Quick reference

```ts
import { /* see API Reference below */ } from "@suaveplan/font-compress";
```

## Dependencies

Workspace dependencies: `@suaveplan/error`, `@suaveplan/font-convert`, `@suaveplan/types`

## Where to read more

This skill is intentionally short. The package's own documentation is the authoritative source — read these in order:

1. **`packages/fonts/font-compress/README.md`** — overview, install, quick-start, module index
2. **`packages/fonts/font-compress/src/**/*.md`** — co-located docs per implementation file (Purpose, Features, Basic + Advanced examples, full API reference, Implementation notes — all packages in genesis ship ≥200 words per module)
3. **`packages/fonts/font-compress/package.json`** `exports` map — every subpath you can import from

When the user asks an API question, read the relevant co-located `.md` first; do not answer from training-data memory.

## See also

- `@suaveplan/error`
- `@suaveplan/font-convert`
- `@suaveplan/types`

---

_This SKILL.md is auto-generated by `suaveplan-skills/scripts/generate-skills.ts` from the package's README. Do not edit by hand — re-run the generator after the README changes._
