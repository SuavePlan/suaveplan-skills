---
name: suppression
description: Use when implementing or debugging code that uses @suaveplan/suppression: Email suppression list management: in-memory store, bulk import/export (CSV/JSON), domain-level suppression, and OTel instrumentation.
---

# @suaveplan/suppression

Email suppression list management for the Suaveplan monorepo. Stores and checks email addresses that should not receive outgoing mail — unsubscribes, hard/soft bounces, complaints, manual suppressions, and invalid addresses. Supports domain-level blocking, bulk CSV/JSON import and export, OTel instrumentation, and full serialisation round-trips.

## When to use this skill

- Implementing or debugging code that imports `@suaveplan/suppression`.
- The task touches: see package README.
- You need API guidance for this package's exports.

## Quick reference

```ts
import { /* see API Reference below */ } from "@suaveplan/suppression";
```

## Dependencies

Workspace dependencies: `@suaveplan/error`, `@suaveplan/types`

## Where to read more

This skill is intentionally short. The package's own documentation is the authoritative source — read these in order:

1. **`packages/email/suppression/README.md`** — overview, install, quick-start, module index
2. **`packages/email/suppression/src/**/*.md`** — co-located docs per implementation file (Purpose, Features, Basic + Advanced examples, full API reference, Implementation notes — all packages in genesis ship ≥200 words per module)
3. **`packages/email/suppression/package.json`** `exports` map — every subpath you can import from

When the user asks an API question, read the relevant co-located `.md` first; do not answer from training-data memory.

## See also

- `@suaveplan/error`
- `@suaveplan/types`

---

_This SKILL.md is auto-generated by `suaveplan-skills/scripts/generate-skills.ts` from the package's README. Do not edit by hand — re-run the generator after the README changes._
